---
title: SecLists
description: The standard wordlist collection used across almost every tool on this site.
---

**SecLists** is the standard wordlist collection used across almost every
tool on this site (directories, subdomains, parameters, etc.).

## Install

```bash
cd /usr/share/wordlists && git clone https://github.com/danielmiessler/SecLists
```

## Notes

Tool pages in this reference use paths under
`/usr/share/wordlists/SecLists/...`, matching the `Install` command above —
adjust if you cloned it somewhere else (e.g. `/opt/SecLists`).

<Cards>
  <Card title="SecLists on GitHub" href="https://github.com/danielmiessler/SecLists">
    The wordlist collection itself.
  </Card>
</Cards>
